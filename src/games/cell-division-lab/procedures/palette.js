// Chromosome tints shared by the procedures. Distinct enough to tell apart,
// dark enough to read on the cream stage, and every one is paired with a
// letter label so colour is never the only thing carrying meaning.

export const CHROM_COLORS = ['#8E44AD', '#C0392B', '#1E8449', '#B9770E'];
export const CHROM_LABELS = ['A', 'B', 'C', 'D'];

// Maternal / paternal, for the meiosis procedures.
export const HOMOLOG_COLORS = ['#8E44AD', '#2874A6'];
